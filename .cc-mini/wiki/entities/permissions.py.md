# Entity: src/core/permissions.py

## Classes
- **class PermissionChecker**: Read-only tools are auto-allowed. Bash/writes prompt the user (y/n/always).
  - Methods: __init__, set_plan_manager, set_esc_listener, check, _prompt_user

## Functions
